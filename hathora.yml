types:
  LevelModifier:
    attach: int
    defence: int
  Action: 
    on: string
    performOn: int[]
    eventType: LevelModifier
    eventValue: string
  Card:
    type: string
    name: string
    actions: Action[]
    levels: LevelModifier
    canApplyToOthers: int
    cardLevel: int
  CardState:
    card: Card
    level: int
    attack: int
    defence: int
    actions: Action[]
  PlayerState:
    userId: UserId?
    deck: CardState[]
    lives: int
    cash: int
    pickableCards: CardState[]
    selectedCardLocation: string?
    selectedCardPosition: int?
    attachBonus: int
    defenceBonuse: int
  UserState:
    userId: UserId?
    lives: int?
    deck: CardState[]
    cash: int?
    selectedCardLocation: string?
    selectedCardPosition: int?
    pickableCards: CardState[]
    opponentUserId: UserId?
    opponentlives: int?
    opponentPickableCards: CardState[]

methods:
  joinGame:
  startGame:
  pickCard:
    pickCard: Card
  applyCard:
    toCard: Card
  endTurn:

auth:
  anonymous: {}

userState: UserState
error: string
